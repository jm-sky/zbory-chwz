Lint, Type-check, and Black
- Run `pnpm lint` to check and fix linting errors in the frontend
- Run `pnpm type-check` to verify TypeScript types in the frontend
- Run `black .` in the `backend/` directory to format Python code
- If there are errors, fix them before proceeding
